import 'package:covid19app/app/modules/analisy/analisy_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(AnalisyModule());
  // AnalisyController analisy;
  //
  setUp(() {
    //     analisy = AnalisyModule.to.get<AnalisyController>();
  });

  group('AnalisyController Test', () {
    //   test("First Test", () {
    //     expect(analisy, isInstanceOf<AnalisyController>());
    //   });

    //   test("Set Value", () {
    //     expect(analisy.value, equals(0));
    //     analisy.increment();
    //     expect(analisy.value, equals(1));
    //   });
  });
}

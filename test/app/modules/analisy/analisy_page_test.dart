import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('AnalisyPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AnalisyPage(title: 'Analisy')));
    //  final titleFinder = find.text('Analisy');
    //  expect(titleFinder, findsOneWidget);
  });
}

import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('CitizenInfoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CitizenInfoPage(title: 'CitizenInfo')));
    //  final titleFinder = find.text('CitizenInfo');
    //  expect(titleFinder, findsOneWidget);
  });
}

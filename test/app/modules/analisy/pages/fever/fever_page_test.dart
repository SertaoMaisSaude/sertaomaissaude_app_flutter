import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('FeverPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FeverPage(title: 'Fever')));
    //  final titleFinder = find.text('Fever');
    //  expect(titleFinder, findsOneWidget);
  });
}

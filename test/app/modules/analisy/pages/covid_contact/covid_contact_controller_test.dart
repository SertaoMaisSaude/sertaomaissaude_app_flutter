import 'package:covid19app/app/modules/analisy/analisy_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(AnalisyModule());
  // CovidContactController covidcontact;
  //
  setUp(() {
    //     covidcontact = AnalisyModule.to.get<CovidContactController>();
  });

  group('CovidContactController Test', () {
    //   test("First Test", () {
    //     expect(covidcontact, isInstanceOf<CovidContactController>());
    //   });

    //   test("Set Value", () {
    //     expect(covidcontact.value, equals(0));
    //     covidcontact.increment();
    //     expect(covidcontact.value, equals(1));
    //   });
  });
}

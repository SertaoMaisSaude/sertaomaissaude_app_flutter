import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('CovidContactPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CovidContactPage(title: 'CovidContact')));
    //  final titleFinder = find.text('CovidContact');
    //  expect(titleFinder, findsOneWidget);
  });
}

import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('ComorbiditiesPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ComorbiditiesPage(title: 'Comorbidities')));
    //  final titleFinder = find.text('Comorbidities');
    //  expect(titleFinder, findsOneWidget);
  });
}

import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('SymptonsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SymptonsPage(title: 'Symptons')));
    //  final titleFinder = find.text('Symptons');
    //  expect(titleFinder, findsOneWidget);
  });
}

import 'package:covid19app/app/modules/analisy/analisy_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(AnalisyModule());
  // SymptonsController symptons;
  //
  setUp(() {
    //     symptons = AnalisyModule.to.get<SymptonsController>();
  });

  group('SymptonsController Test', () {
    //   test("First Test", () {
    //     expect(symptons, isInstanceOf<SymptonsController>());
    //   });

    //   test("Set Value", () {
    //     expect(symptons.value, equals(0));
    //     symptons.increment();
    //     expect(symptons.value, equals(1));
    //   });
  });
}

import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('DirectionPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DirectionPage(title: 'Direction')));
    //  final titleFinder = find.text('Direction');
    //  expect(titleFinder, findsOneWidget);
  });
}

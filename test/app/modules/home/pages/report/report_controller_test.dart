import 'package:covid19app/app/modules/home/home_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(HomeModule());
  // ReportController report;
  //
  setUp(() {
    //     report = HomeModule.to.get<ReportController>();
  });

  group('ReportController Test', () {
    //   test("First Test", () {
    //     expect(report, isInstanceOf<ReportController>());
    //   });

    //   test("Set Value", () {
    //     expect(report.value, equals(0));
    //     report.increment();
    //     expect(report.value, equals(1));
    //   });
  });
}

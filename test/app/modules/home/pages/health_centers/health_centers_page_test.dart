import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('HealthCentersPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(HealthCentersPage(title: 'HealthCenters')));
    //  final titleFinder = find.text('HealthCenters');
    //  expect(titleFinder, findsOneWidget);
  });
}

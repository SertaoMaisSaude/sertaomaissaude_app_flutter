import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('HealthTipsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(HealthTipsPage(title: 'HealthTips')));
    //  final titleFinder = find.text('HealthTips');
    //  expect(titleFinder, findsOneWidget);
  });
}

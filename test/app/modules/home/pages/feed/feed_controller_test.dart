import 'package:covid19app/app/modules/home/home_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(HomeModule());
  // FeedController feed;
  //
  setUp(() {
    //     feed = HomeModule.to.get<FeedController>();
  });

  group('FeedController Test', () {
    //   test("First Test", () {
    //     expect(feed, isInstanceOf<FeedController>());
    //   });

    //   test("Set Value", () {
    //     expect(feed.value, equals(0));
    //     feed.increment();
    //     expect(feed.value, equals(1));
    //   });
  });
}

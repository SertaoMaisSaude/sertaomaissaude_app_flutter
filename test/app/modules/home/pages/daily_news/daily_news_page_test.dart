import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('DailyNewsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DailyNewsPage(title: 'DailyNews')));
    //  final titleFinder = find.text('DailyNews');
    //  expect(titleFinder, findsOneWidget);
  });
}
